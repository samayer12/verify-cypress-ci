name: Verify Cypress

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    continue-on-error: true

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run verify-cypress.sh script
      run: bash verify-cypress.sh

